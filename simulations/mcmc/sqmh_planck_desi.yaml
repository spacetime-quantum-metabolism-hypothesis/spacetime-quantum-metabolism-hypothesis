# Cobaya config for Phase 3 SQMH joint MCMC
# STUB: Phase 2 CLASS 패치 완료 후 활성화
#
# Usage (after Phase 2 done):
#   cobaya-run sqmh_planck_desi.yaml -r 4

theory:
  classy:
    # Replace with sqmh_theory_wrapper.SQMHClassy after Phase 2
    path: ../class_patch/class_sqmh
    extra_args:
      # SQMH-specific parameters (injected via patched CLASS)
      SQMH_enabled: 1
      SQMH_V_family: RP    # RP | mass | exp
      # SQMH_xi, SQMH_V_params sampled below
      N_ncdm: 1
      N_ur: 2.0328
      m_ncdm: 0.06
      T_ncdm: 0.71611

likelihood:
  # Planck 2018 TTTEEE + lensing
  planck_2018_highl_plik.TTTEEE:
  planck_2018_lowl.TT:
  planck_2018_lowl.EE:
  planck_2018_lensing.clik:

  # DESI DR2 BAO
  bao.desi_2024_bao_all:   # TODO: DR2 release name in Cobaya once published

  # DESY5 SN
  sn.desy5:

params:
  # --- Cosmological ---
  H0:
    prior: {min: 55, max: 80}
    ref: {dist: norm, loc: 67.36, scale: 1.0}
    proposal: 0.5
    latex: H_0

  omega_b:
    prior: {min: 0.019, max: 0.025}
    ref: {dist: norm, loc: 0.0224, scale: 0.0001}
    proposal: 0.0001
    latex: \Omega_\mathrm{b} h^2

  omega_cdm:
    prior: {min: 0.10, max: 0.14}
    ref: {dist: norm, loc: 0.120, scale: 0.001}
    proposal: 0.0005
    latex: \Omega_\mathrm{c} h^2

  A_s:
    prior: {min: 1.5e-9, max: 3e-9}
    ref: {dist: norm, loc: 2.1e-9, scale: 1e-11}
    proposal: 1e-11
    latex: A_\mathrm{s}

  n_s:
    prior: {min: 0.9, max: 1.0}
    ref: {dist: norm, loc: 0.9649, scale: 0.005}
    proposal: 0.002
    latex: n_\mathrm{s}

  tau_reio:
    prior: {min: 0.02, max: 0.12}
    ref: {dist: norm, loc: 0.054, scale: 0.008}
    proposal: 0.003
    latex: \tau_\mathrm{reio}

  # --- SQMH ---
  SQMH_xi:
    prior: {min: -1.0, max: 1.0}
    ref: {dist: norm, loc: 0.35, scale: 0.1}
    proposal: 0.05
    latex: \xi_\mathrm{SQMH}

  SQMH_V_param_n:       # Ratra-Peebles slope
    prior: {min: 0.01, max: 5.0}
    ref: {dist: norm, loc: 0.35, scale: 0.1}
    proposal: 0.05
    latex: n_\mathrm{RP}

  # --- Derived ---
  sigma8:
    latex: \sigma_8
  omega_m:
    latex: \Omega_\mathrm{m}
  S8:
    latex: S_8
  w0_eff:
    latex: w_0^\mathrm{eff}
  wa_eff:
    latex: w_a^\mathrm{eff}

sampler:
  mcmc:
    Rminus1_stop: 0.05
    Rminus1_cl_stop: 0.2
    max_tries: 1000
    learn_proposal: true
    oversample_power: 0.4

output: chains/sqmh_planck_desi_RP
