# paper/figures/ — F0–F9 specifications

Generated by `results/L443/make_figures.py` (L443, 2026-05-01).

| ID  | File                          | Status                | Source / Note |
|-----|-------------------------------|-----------------------|---------------|
| F0  | F0_axiom_graph.png            | real (matplotlib)     | static graph: 6 axioms (L0–L5) → 5 derived predictions (D1–D5). Hand-curated edges. |
| F1  | F1_placeholder.png            | placeholder           | spec: σ₀(env) 3-regime + non-monotonic dip, log-scale, colour-coded. Aggregator pending. |
| F2  | F2_placeholder.png            | placeholder           | spec: SPARC fit examples (3 galaxies), 3-panel + residual. Standardisation pending. |
| F3  | F3_placeholder.png            | placeholder           | spec: a₀ = c·H₀/(2π) derivation, disc projection, vector graphic (manual). |
| F4  | F4_placeholder.png            | placeholder           | spec: Bullet cluster mass profile — X-ray + stellar + lensing overlay. |
| F5  | F5_rho_q_evolution.png        | real (uses L207 data) | inputs: `results/L207/report.json` (`rho_q_0`, `absorption_per_hubble = 0.104`). Bianchi toy: ρ_q(z)/ρ_q(0) = a^(-3·s). |
| F6  | F6_gmm_sparc.png              | schematic             | 2-component GMM placeholder over synthetic log_σ₀ sample. **Replace with real SPARC GMM fit** before submission. |
| F7  | F7_mock_injection.png         | schematic             | null vs injected mock ΔAICc histograms + dummy real-fit line at −2.9. **Replace with real mock pipeline output**. |
| F8  | F8_placeholder.png            | placeholder           | spec: AIC/BIC/DIC/WAIC grouped bar. Aggregator pending. |
| F9  | F9_facility_gantt.png         | real (manual)         | DESI DR3/DR4, Euclid DR1/DR3, Rubin LSST Y1/Y10, CMB-S4, LiteBIRD, SKA1 — manually curated 2025–2034 timeline. |

## Honesty notes

- F0, F5, F9 are publishable scientific content (F5 ties to L207 saved JSON).
- F6 and F7 are **schematic placeholders** with synthetic data — they show the
  intended layout but must be regenerated from the real SPARC GMM fit and
  mock-injection pipeline before paper submission. Both PNGs carry an inline
  "schematic" caption for safety.
- F1, F2, F3, F4, F8 are pure placeholders flagged for future aggregator /
  vector-graphic work.

## How to regenerate

```bash
python3 results/L443/make_figures.py
```

All outputs land in `paper/figures/F*.png`.
